name: Large Seed Checksum Verification

# Disabled - checksum and formats directories removed during refactoring
# This workflow references removed directories and scripts

on:
  workflow_dispatch:

jobs:
  skip:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Skip message
      run: |
        echo "This workflow has been disabled as part of the DRBG refactoring."
        echo "The checksum/ and formats/ directories were removed to focus on core functionality."
        echo "Core functionality is validated through the main test suite."
jobs:
  skip:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Skip message
      run: |
        echo "This workflow has been disabled as part of the DRBG refactoring."
        echo "The checksum/ and formats/ directories were removed to focus on core functionality."
        echo "Core functionality is validated through the main test suite."
