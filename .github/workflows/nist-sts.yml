name: NIST Statistical Test Suite

# Disabled - NIST testing scripts removed during refactoring
# This workflow references removed scripts and data directories

on:
  workflow_dispatch:

jobs:
  skip:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Skip message
      run: |
        echo "This workflow has been disabled as part of the DRBG refactoring."
        echo "NIST validation scripts were removed to focus on core functionality."
        echo "Core DRBG functionality is validated through the main test suite."
jobs:
  skip:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Skip message
      run: |
        echo "This workflow has been disabled as part of the DRBG refactoring."
        echo "NIST validation scripts were removed to focus on core functionality."
        echo "Core DRBG functionality is validated through the main test suite."
